
spring:
  cloud:
    config:
      uri: http://localhost:8081
  application:
        name: zuul

eureka:
    client:
        fetchRegistry: true
        registerWithEureka: true
        serviceUrl:
            defaultZone: http://localhost:8761/eureka
server:
  port: 8083